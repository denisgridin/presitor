export enum LAYOUTS {
  EMPTY = 'empty',
  DEFAULT = 'default',
  AUTH = 'auth',
  APP = 'app'
}

export enum AUTH_TYPE {
  registration = 'registration',
  login = 'login'
}

export enum ACTION_TAB {
  ELEMENTS = 'elements',
  FEEDBACK = 'feedback'
}

export enum ELEMENT_TYPE {
  square = 'square',
  circle = 'circle',
  rectangle = 'rectangle'
}
